'use strict';

const OutputDevice = require('../output');

module.exports = class PulseDevice extends OutputDevice {

  onInit(options = {}) {
    super.onInit(options);
  }

  // TODO: implement !!

};
